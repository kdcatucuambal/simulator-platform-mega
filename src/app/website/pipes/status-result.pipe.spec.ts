import { StatusResultPipe } from './status-result.pipe';

describe('StatusResultPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusResultPipe();
    expect(pipe).toBeTruthy();
  });
});
