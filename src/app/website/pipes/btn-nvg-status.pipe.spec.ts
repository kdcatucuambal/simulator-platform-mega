import { BtnNvgStatusPipe } from './btn-nvg-status.pipe';

describe('BtnNvgStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new BtnNvgStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
