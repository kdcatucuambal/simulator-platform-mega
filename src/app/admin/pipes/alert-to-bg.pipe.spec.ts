import { AlertToBgPipe } from './alert-to-bg.pipe';

describe('AlertToBgPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertToBgPipe();
    expect(pipe).toBeTruthy();
  });
});
